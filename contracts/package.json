{
  "name": "protrack-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for ProTrack supply chain management",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:protrack": "hardhat run scripts/deploy-protrack.js",
    "deploy:localhost": "hardhat run scripts/deploy-protrack.js --network localhost",
    "node": "hardhat node"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "@chainlink/contracts": "^0.8.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  }
}
